%div.form-group
  = simple_form_for(@race) do |f|
    = f.input :name, :input_html => {:class => 'form-control'}
    = f.input :game_id, :collection => Game.all, :input_html => {:class => 'form-control'}
    = f.submit